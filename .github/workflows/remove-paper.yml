name: Remove paper from issue

# Fill in the "🗑️ Remove a paper" issue form -> this deletes it + regenerates the README.
# `labeled` (not `opened`): the form applies the remove-paper label on submit, so labeled
# fires once. Listening on opened too would double-fire and race two runs on the push.
on:
  issues:
    types: [labeled, edited]

permissions:
  contents: write
  issues: write

jobs:
  remove:
    if: contains(join(github.event.issue.labels.*.name, ','), 'remove-paper')
    runs-on: ubuntu-latest
    concurrency: edit-papers          # share the lane with add-paper: no racing on papers.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pyyaml

      - name: Resolve + remove paper
        id: remove
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: python scripts/remove_from_issue.py

      - name: Commit + push
        id: push
        if: steps.remove.outputs.status == 'removed'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ README.md          # data/ so a benchmark/survey deletion is committed too
          git commit -m "Remove paper from #${{ github.event.issue.number }}" \
            -m "Closes #${{ github.event.issue.number }}"
          # retry the push a few times so a lost concurrency race doesn't drop the change
          for i in 1 2 3 4 5; do
            git push && exit 0
            git pull --rebase origin main
          done
          git push

      - name: Comment result
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BODY=$(cat <<'__EOF__'
          ${{ steps.remove.outputs.summary }}
          __EOF__
          )
          [ -z "$BODY" ] && BODY="Something went wrong before the paper could be processed — check the workflow logs."
          gh issue comment ${{ github.event.issue.number }} --body "$BODY" --repo "$GITHUB_REPOSITORY"

      - name: Close issue (only if the change actually landed)
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          STATUS='${{ steps.remove.outputs.status }}'
          PUSH='${{ steps.push.outcome }}'   # success | failure | skipped
          # `removed` must have a successful push; `not_found` has nothing to push.
          if [ "$STATUS" = "not_found" ] || { [ "$STATUS" = "removed" ] && [ "$PUSH" = "success" ]; }; then
            gh issue close ${{ github.event.issue.number }} --repo "$GITHUB_REPOSITORY" || true
          fi
